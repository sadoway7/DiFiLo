// Package db provides all SQLite database operations for DiFiLo:
// users, comments, bookmarks, settings, downloads, content pages,
// full-text search, and the wiki lifecycle (revisions, proposals,
// maintainers, tags, notifications, uploads).
//
// This package depends only on textutil (for snippet generation in
// the search fallback path).
package db
